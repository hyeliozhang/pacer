# Environment used for the PACER artifact checks.
# The prototype is CPU-only; no external service is required.
python==3.11
numpy>=1.24,<3
matplotlib>=3.7,<4
scikit-learn>=1.3,<2
pytest>=8,<10
system: POSIX shell, standard build tools
