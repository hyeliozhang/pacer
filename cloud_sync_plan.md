# Cloud Sync Plan

The repository is intended to be pushed as a clean artifact checkpoint. Keep the paper tree, local check outputs, credentials, and personal notes outside the remote repository. Regenerate heavy experiment artifacts only through the documented scripts or a clean CPU-only container.
